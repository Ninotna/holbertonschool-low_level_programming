#include "main.h"
/**
 * print_square - function that prints a square, followed by a new line.
 * @size: number of \\
 * Return: print square
 */

void print_square(int size)
{
	int i, j;

	if (n > 0)
	{
		for (j = 1; j <= n; j++)
		{
			for (i = 1; i <= n; i++)
			{
				_putchar('#');
				_putchar('\n');
				}
			}
		}
	}
	else
		_putchar('\n');
}
