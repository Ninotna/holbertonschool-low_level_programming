#include "main.h"
/**
 * add - function that adds two integers and returns the result.
 * @a : integer
 * @b : b
 * Return: addi a + b
 */
int add(int a, int b)
{
	int addi = a + b;

	return (addi);
}
